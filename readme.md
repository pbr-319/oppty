# opensslkey generation cmd
ssh-keygen -t rsa -b 4096

# git cmd
git clone git@github.com:pbr-319/oppty.git
git config --global user.email "poreddybhargavareddy319@gmail.com"
git config --global user.name "pbr-319"

git status
git add .
git status
git commit -m "initial code push"
git push